import { Controller } from '@nestjs/common';

// incoming req and returning res
// http://localhost:5000/users

@Controller('users')
export class UsersController {
  // dependency injection
  //constructor(private readonly )
}
